services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - dbdata:/app/data
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G

volumes:
  dbdata:
